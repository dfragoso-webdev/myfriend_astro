---
// src/features/landing/Landing.astro
import About from "./components/About/About";
import Banner from "./components/Banner/Banner";
import BranchesCarousel from "./components/Branches/BranchesCarousel";
import FollowUs from "./components/FollowUs/FollowUs";
import HomeService from "./components/HomeService/HomeService";
import LuxeIslandBand from "./components/LuxeIslandBand/LuxeIslandBand";
import Products from "../products/Products";
import PremiumSelect from "../products/PremiumSelect";
import Stores from "./components/Stores/Stores";
import { video as videoAssets } from "@/mediaRoutes";

interface Props {
  lang: string;
  bannerImages?: { images: string[]; imagesMobile: string[] };
  branches?: any[];
  videoSrc?: string;
  bg?: string;
}

const {
  lang,
  bannerImages,
  branches,
  videoSrc = videoAssets.myfriend,
  bg = "",
} = Astro.props;
---

<main>
  <section id="home">
    <Banner
      images={bannerImages?.images || []}
      imagesMobile={bannerImages?.imagesMobile || []}
      client:only="react"
    />
  </section>

  <section id="about-us">
    <About videoSrc={videoSrc} bg={bg} client:visible />
  </section>

  <section id="stores">
    <Stores lang={lang} client:visible />
  </section>

  <section id="premium-select">
    <PremiumSelect client:visible />
  </section>

  <section id="products">
    <Products client:visible />
  </section>

  <section id="home-service">
    <HomeService client:visible  />
  </section>

  <section id="luxe-island">
    <LuxeIslandBand client:visible />
  </section>

  <section id="follow-us">
    <FollowUs client:visible />
  </section>

  <BranchesCarousel branches={branches || []} lang={lang} client:visible />
</main>

<script>
  if (window.location.hash) {
    window.history.pushState(
      {},
      "",
      window.location.pathname + window.location.search,
    );
    window.scrollTo({ top: 0, behavior: "instant" });
  }
</script>
